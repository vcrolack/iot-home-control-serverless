const validRoles = ['admin', 'users'];
const validRolesTest = ['admin-test', 'users-test'];

const adminTest = 'admin-test';
const usersTest = 'users-test';
const admin = 'admin';
const users = 'users';

module.exports = {
  validRoles,
  validRolesTest,
  adminTest,
  usersTest,
  admin,
  users
};